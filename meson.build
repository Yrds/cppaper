project('blogcpp', 'cpp', version: '0.0.1', default_options: ['cpp_std=c++20'])

src = files(
  'src/main.cpp'
  )

inc = include_directories('src')

executable('blogcpp', src, include_directories: inc )
